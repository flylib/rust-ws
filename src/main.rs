// main.rs is unused — see src/bin/server_demo.rs and src/bin/client_demo.rs.
// Keep this file so `cargo build` has a default binary target.
fn main() {
    eprintln!("Run `cargo run --bin server-demo` or `cargo run --bin client-demo`");
}
